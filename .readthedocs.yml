version: 2

python:
   version: 3.8
   install:
      - method: pip
        path: .
        extra_requirements:
            - docs

build:
    os: ubuntu-22.04
    tools:
    python: "3.12"

sphinx:
  builder: html
  configuration: docs/conf.py
  fail_on_warning: true
